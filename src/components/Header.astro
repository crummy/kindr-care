---
import Icon from "./Icon.astro";
import { navLinks } from "@content/site";
---

<header>
  <div class="wrap nav">
    <a class="logo" href="#top" aria-label="Kindr home">kindr<span>.</span></a>
    <nav class="nav-links" aria-label="Primary navigation">
      {navLinks.map((link) => <a class="text-link" href={link.href}>{link.label}</a>)}
      <div class="nav-cta">
        <a class="btn btn-outline" href="#contact">Gift Cards</a>
        <a class="btn btn-primary" href="#janet">Book with Janet</a>
      </div>
    </nav>
    <button class="menu-toggle" type="button" aria-label="Open menu" data-menu-toggle>
      <Icon name="menu" />
    </button>
  </div>
  <nav class="mobile-nav" aria-label="Mobile navigation" data-mobile-nav hidden>
    {navLinks.map((link) => <a href={link.href}>{link.label}</a>)}
    <a href="#contact">Gift Cards</a>
    <a href="#janet">Book with Janet</a>
  </nav>
</header>
