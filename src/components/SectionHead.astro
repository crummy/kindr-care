---
interface Props {
  kicker: string;
  title: string;
}

const { kicker, title } = Astro.props;
---

<div class="section-head">
  <span class="kicker">{kicker}</span>
  <h2>{title}</h2>
  <slot />
</div>
